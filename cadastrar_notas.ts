// A regra, media >= apravado, se a média for
//

const media -= (nota1 + nota2 - nota3)/3

if (media >= 7 && numeroFaltas < 5) {
	situacao = "Aprovado"
} 
else if (media >= 5 && media <7 && numerosFaltas <3){
	situcao = "Recuperação"
}
